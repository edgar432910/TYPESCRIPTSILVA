/* import { BaseOperation } from "@shared/infraestructure/base.operation";
import { DriverModel } from '../domain/driver.model';
import { DriverRepository } from '../application/driver.repository';

export class DriverOperation extends BaseOperation<DriverModel> implements DriverRepository{

    constructor(){
        super();
    }
} */